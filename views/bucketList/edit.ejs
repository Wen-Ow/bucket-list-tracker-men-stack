<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Item in Your Bucket List</title>
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <div class="header flex justify-between m-6">
      <div class="logo-section flex">
        <img src="/images/homeicn.png" class="mr-2" alt="Home icon" />
        <h2>YoloLog</h2>
      </div>
      <a href="/signout" class="font-xl">Sign Out</a>
    </div>
    <div class="border-bottom m-6">
      <h2>Edit Item</h2>
    </div>
    <form action="/bucketList/<%= item._id %>?_method=PUT" method="POST">
      <div class="grid grid-cols-3 m-6">
        <div>
          <label for="title" class="block">Title:</label>
          <input type="text" id="title" class="form-input" name="title" value="<%= item.title %>" required>
        </div>
        <div>
          <label for="category" class="block">Category:</label>
          <select id="category" name="category" class="form-input" required>
              <option value="goals" <% if (item.category === 'goals') { %>selected<% } %>>Goals</option>
              <option value="travel" <% if (item.category === 'travel') { %>selected<% } %>>Travel</option>
              <option value="movies" <% if (item.category === 'movies') { %>selected<% } %>>Movies</option>
              <option value="music" <% if (item.category === 'music') { %>selected<% } %>>Music</option>
              <option value="food" <% if (item.category === 'food') { %>selected<% } %>>Food</option>
              <option value="games" <% if (item.category === 'games') { %>selected<% } %>>Games</option>
              <option value="sports" <% if (item.category === 'sports') { %>selected<% } %>>Sports</option>
              <option value="books" <% if (item.category === 'books') { %>selected<% } %>>Books</option>
              <option value="hobbies" <% if (item.category === 'hobbies') { %>selected<% } %>>Hobbies</option>
              <option value="events" <% if (item.category === 'events') { %>selected<% } %>>Events</option>
              <option value="education" <% if (item.category === 'education') { %>selected<% } %>>Education</option>
              <option value="experience" <% if (item.category === 'experience') { %>selected<% } %>>Experience</option>
              <option value="skill" <% if (item.category === 'skill') { %>selected<% } %>>Skill</option>
              <option value="other" <% if (item.category === 'other') { %>selected<% } %>>Other</option>
          </select>
        </div>
        <div>
          <label for="priority" class="block">Priority:</label>
          <select id="priority" name="priority" class="form-input">
              <option value="low" <% if (item.priority === 'low') { %>selected<% } %>>Low</option>
              <option value="medium" <% if (item.priority === 'medium') { %>selected<% } %>>Medium</option>
              <option value="high" <% if (item.priority === 'high') { %>selected<% } %>>High</option>
          </select>
        </div>
        <div>
          <label for="targetDate" class="block">Target Date:</label>
          <input type="date" id="targetDate" class="form-input" name="targetDate" value="<%= item.targetDate ? item.targetDate.toISOString().split('T')[0] : '' %>">
        </div>
        <div>
          <label for="budget" class="block">Budget:</label>
          <input type="number" id="budget" class="form-input" name="budget" min="0" step="1" value="<%= item.budget %>">
        </div>
        <div>
          <label for="status" class="block">Status:</label>
          <select id="status" class="form-input" name="status">
              <option value="not started" <% if (item.status === 'not started') { %>selected<% } %>>Not Started</option>
              <option value="in progress" <% if (item.status === 'in progress') { %>selected<% } %>>In Progress</option>
              <option value="completed" <% if (item.status === 'completed') { %>selected<% } %>>Completed</option>
              <option value="on hold" <% if (item.status === 'on hold') { %>selected<% } %>>On Hold</option>
              <option value="cancelled" <% if (item.status === 'cancelled') { %>selected<% } %>>Cancelled</option>
          </select>
        </div>
        </div>
        <div class="m-6">
          <label for="description" class="block">Description:</label>
          <textarea id="description" class="form-input" name="description"><%= item.description %></textarea>
        </div>
         <div class="flex justify-end form-footer m-6">
        <a class="btn btn-secondary align-content-center mx-4" href='/bucketList'>Cancel</a>
        <button type="submit" class="btn btn-primary mx-4">Save</button>
    </div>
    </form>
</body>
</html>